apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rancher
  namespace: cattle-system
spec:
  secretName: tls-rancher-ingress

  dnsNames:
    - rancher.cyrostack.dev

  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns01